apiVersion: apps/v1
kind: Deployment
metadata:
  name: CAPSTIONE
  labels:
    app: CAPSTIONE
  namespace: default
spec:
  replicas: 5
  selector:
    matchLabels:
      app: CAPSTIONE
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: CAPSTIONE
    spec:
      containers:
        - image: rajkumar080/capstondockerapp
          imagePullPolicy: Always
          name: CAPSTIONE
          ports:
            - containerPort: 80
          readinessProbe:
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 1
            httpGet:
              path: /
              port: 80